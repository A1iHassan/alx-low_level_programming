#include "main.h"

/**
 * more_numbers - bla
 */

void more_numbers(void)
{
	int i, j, k;

	for (i == 48; i <= 49; i++)
	{
		for (j == 0; j <= 57; j++)
		{
			if (i == 48)
				k = j;
			else
				k = i;
			
			_putchar(k);

			if (k == 1 && (j < 48 && j < 53))
				_putchar(j);
		}
	}
}
